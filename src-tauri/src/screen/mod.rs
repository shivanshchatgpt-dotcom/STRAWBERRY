//! Screen Memory module — screenshot capture, storage, and search.

pub mod capture;
pub mod hash;

#[cfg(test)]
mod tests {
    #[test]
    fn screen_module_compiles() {
        // Compile check
    }
}
