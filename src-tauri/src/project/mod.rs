//! 🌳 Project Brain — Phase C (aggregation) + Phase E (what changed)
//! + Phase D (intelligent resume).
//!
//! Pure read-only aggregation over existing storage — no new tables.

pub mod brain;
pub mod changed;
pub mod resume_narrative;
